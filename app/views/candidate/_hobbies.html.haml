/ decorator_modal
= modal_tag do
  = modal_header do
    = modal_image_tag :hobbies
    %h2.title Hobbies
    .clear
  = modal_body do
    %ul
      - candidate.hobbies.each do |h|
        %li= h.name
