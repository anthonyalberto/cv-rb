/ decorator_modal
= modal_tag do
  = modal_header do
    = modal_image_tag :skills
    %h2.title Skills
    .clear
  = modal_body do
    %ul
      - candidate.skills.each do |s|
        %li= "#{s.level}: #{s.name}"
