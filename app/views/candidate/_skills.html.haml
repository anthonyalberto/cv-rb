/ decorator_modal
= modal_tag(class: section) do
  = modal_header do
    = modal_image_tag :skills
    %h2.title= t("candidate.title.skills")
    .clear
  = modal_body do
    %ul
      - candidate.skills.each do |s|
        %li= "#{s.level}: #{s.name}"
