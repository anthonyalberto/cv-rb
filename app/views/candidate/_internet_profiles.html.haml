/ decorator_modal
= modal_tag do
  = modal_header do
    = modal_image_tag :internet_profiles
    %h2.title= t("candidate.title.internet_profiles")
    .clear
  = modal_body do
    %ul
      - candidate.profile_links.each do |p|
        %li= link_to p.name, p.url, target: "_blank"
